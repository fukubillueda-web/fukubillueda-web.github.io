# Fukuhiro Ueda — academic homepage

Static GitHub Pages site for Fukuhiro Ueda.

- `index.html` contains the homepage and publication list.
- `style.css` contains the site styling.
- `papers/` contains the PDFs linked from the homepage.

The site is intentionally plain HTML/CSS with no build system, so it can be edited directly and should remain easy to maintain.
